<div class="trends">
  <div class="total-items">
    <h3>Number of Content Items</h3>
    <div class="chart-stats">
      <div class="total">Total
        = <%= number_with_delimiter (@metrics.count('distinct dimensions_items.content_id') || 0) %></div>
    </div>
    <%= line_chart(@metrics.group('facts_metrics.dimensions_date_id').count, height: '150px') %>
  </div>

  <%= render 'section', metric: params[:metric1] if params[:metric1]%>
  <%= render 'section', metric: params[:metric2] if params[:metric2] %>
  <%= render 'section', metric: params[:metric3] if params[:metric3] %>
</div>
